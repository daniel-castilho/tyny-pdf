# Epic 2 – Stories (Acceptance) [grounded]

Each AC cites the real file (ADR, doc, tool, brief row) it comes from, quotes the brief's own
target where the brief states one, and ends in a check that is executable or in the reason it is
not executable yet. `epic-2-dod.md` requires a pasted command output next to every figure,
including a figure that says "could not be measured". A `[x]` here or there, without that output
above it, is forbidden by `AGENTS.md` Critical Rule 4.

| # | Story | Acceptance Criteria (grounded) | Real Reference |
|---|-------|--------------------------------|----------------|
| 2.1 | **The spike measures, or it is an opinion** | <br>- The four entry conditions are checked with their commands and the output pasted: `python3 tools/layering-check.py` no longer prints `backend_line_ratio=undefined`; `cmake --preset win-cross-x64 && cmake --build --preset win-cross-x64` produces `build/win-cross-x64/Release/tynypdf.exe`; `sh tools/win-probe/build.sh --probe gpu` prints an adapter LUID that is not the software rasteriser, or ADR-0010's consequence is recorded and every timing number in this epic is labelled "Windows session, physical machine"<br>- The frame source is `tools/bench-measure.py` with `--target tynypdf --binary <path> --corpus <pinned corpus> --runs 2 --record-machine`, whose metrics are open, first paint, `scroll_frame_p99_ms`, search and `peak_rss_kib`; no new measurement tool is written, because a second harness is how two answers to one question appear<br>- The per-frame interval comes from the CLI's JSON reporter (ADR-0002: `src/cli` is "headless, exit codes 0,1,2,3, JSON plus JUnit reporters"), consumed by the existing harness; the app writes the same fields through the `tynypdf.ui` log subsystem named in `docs/naming.md`<br>- Two consecutive runs agree within 10 % per metric, which is the tool's own `TOLERANCE`, and both outputs are pasted; a third run is added rather than an outlier being dropped<br>- The three days are three dated sessions, each recording what it attempted and what it produced; if a fourth is needed, the note says which criterion moved (D6b exists so that the revisit happens with data, not with hope)<br>- `sh tools/check.sh` green at the end of each session, so the spike never becomes the reason `main` is red | `docs/kickoff.md` section 6 M1 and section 11, ADR-0010 probe table row 4, `tools/bench-measure.py`, `docs/naming.md`, `docs/dev-environment.md` |
| 2.2 | **One window, one swapchain, one blit at the frame budget** | <br>- `src/os/win32/window/` owns the window and the message loop and `src/render/swapchain/` owns the DComp visual and the swapchain; neither contains parsing, geometry reconciliation or annotation mutation, and that is asserted by `python3 tools/layering-check.py --strict` exiting 0, not by a reviewer's reading (R-M10)<br>- A 4000x3000 page region is blitted at 60 fps sustained with no frame over 33 ms p99, with the frame table pasted; the number is the one the harness prints, not a stopwatch reading<br>- The same bytes come out of `tynypdf-cli render --page N --out x.png`: the M0.3 mechanism extended to a page region, so a pixel difference is a renderer bug and a hash difference in the window is a presentation bug, and the two cannot be confused<br>- `src/features/render/SPEC.md` is written in the same commit as the first file in that directory, never before it, in the shape ADR-0011 R-M13 requires (one-line EARS statements, an id per requirement, a `Verification:` target), and `python3 tools/spec-check.py` reports no orphan for it<br>- `backend_line_ratio` is reported as a number at or below 0.15 while this code exists, because a spike that reaches into the engine directly is the cheapest way to break the swap budget (R-M11)<br>- `sh tools/check.sh` green | `docs/kickoff.md` M1 row 1, ADR-0002 `src/render` and `src/os/win32` contents, ADR-0011 R-M10/R-M11/R-M13, `tools/layering-check.py` |
| 2.3 | **Tiles, the cachemap, and a budget that lives in the core** | <br>- `src/render/tiles/` and `src/render/cachemap/` hold the mechanism; the decision of what stays resident belongs to `src/core/budget/`, so the render layer reads a budget and never invents one (R-M8: semantics live on the IR, and a budget that lives in the swapchain cannot be unit-tested on Linux)<br>- 1000 pages open at three tiles each holds peak RSS at or below 250 MB, and the return pass does not raise it above the forward pass: both peaks pasted from the same harness run, because "it must not grow when scrolling back" is a statement about the second pass, not about the first<br>- The eviction rule is covered by unit tests that run with no GPU and no window, so a Linux CI job catches a regression in it; the contract for what the GPU holds stays in the render layer's spec<br>- `python3 tools/sbom.py --root . --strict` still exits 3 for the right reason (no `third_party`, no `conan.lock`) rather than being softened, and no new dependency is added to make caching easier - a cache library is a dependency decision and ADR-0004 section 6 wants a register row for it, in the same PR as the first `#include`<br>- `sh tools/check.sh` green | `docs/kickoff.md` M1 row 2 and section 10 (the "Performance target unreachable" response names tile cache sizing and the memory budget struct as the levers), ADR-0011 R-M8, ADR-0004 section 6 |
| 2.4 | **Cold start: the absolute number now, the comparison when the bar exists** | <br>- First painted page measured from process start to the frame that presents page 1, target 300 ms on the reference machine, with the machine block `tools/bench-measure.py --record-machine` prints, so the number can be re-taken rather than re-argued<br>- The relative half of M1 row 3 ("vs measured Sumatra") is reported as **not satisfiable yet** with its reason: `tests/baseline.json` does not exist and `python3 tools/corpus-check.py --root .` exits 3 naming the missing manifest. This is Epic 1 story 1.5, held as owner debt, and the row stays open rather than being redefined as "absolute only"<br>- The corpus used for whatever timing is recorded here is named with the file list and its sha256s; if it is not the pinned corpus, the record says so on the same line as the number, because a number measured on a different document is not comparable to one from the baseline<br>- No status badge, README claim or release note quotes a cold-start figure from this epic as a product claim; the brief's own gate forbids a badge before the job that produces it exists (`docs/kickoff.md` section 11, PR #2)<br>- `sh tools/check.sh` green | `docs/kickoff.md` M1 row 3 and section 7 gate 2 (reality gate), `tools/bench-measure.py`, `tools/corpus-check.py`, `docs/dependency-policy.md` section 0 |
| 2.5 | **DPI, composition, caret, and the 16 ms of a gesture** | <br>- Per-Monitor V2 is declared where the OS layer declares it (`src/os/win32/dpi/`, ADR-0002's list of that directory's responsibilities), and the evidence is two renderings of the same page: the one at the scaled size and the one asked for the scaled size, compared byte-for-byte, so "no bitmap stretch" is a pixel fact and not a manifest string this document could only guess at<br>- The approval files for that comparison go under `tests/approvals/` with the documented accept flow (ADR-0002), not beside the source that produced them<br>- Wheel and pinch track 1:1 with no gesture lag over 16 ms, measured as the interval between the input message and the frame that presents it, printed by the `tynypdf.ui` per-frame line; the pasted log is the deliverable, and a distribution is reported as a distribution (p50 and p99, not an average)<br>- ABNT2 and pt-BR composition works in a text field and caret movement over combining marks is correct: the arithmetic lives in `src/core/text` and is unit-tested without a window; the IME plumbing in `src/os/win32/window/` is tested on the Windows job only, because ADR-0010 forbids assuming an OS behaviour from the Linux side<br>- `sh tools/check.sh` green, including `sh tools/format-check.sh`, since this story writes the first C++ that a reviewer will read for style | `docs/kickoff.md` M1 rows 4, 6, 7, ADR-0002 `src/os/win32` and `src/core/text`, ADR-0010 rule "Windows-specific things are not assumed", `docs/naming.md` log subsystems |
| 2.6 | **Reachable, narrated, and ended with a verdict** | <br>- Every control reachable by keyboard, and Narrator and NVDA announce page, zoom and focus: the first two scripts live in `docs/a11y/` with the exact announced text recorded in the file, so "announces correctly" is a diff and not a memory. `docs/kickoff.md` M5 owns the complete a11y delta; this story owns the floor M1's row 5 names, and the record says which of the two it satisfied<br>- `src/os/win32/uia/` provides the tree those readers consume; the UIA assertions are written where they can run (the Windows job), and nothing about them is inferred from Linux<br>- The verdict is written in one of two shapes and both are a pass for this epic: (a) the seven numbers are in `epic-2-dod.md` and the spike's code becomes the seed of the viewer; (b) the kill criterion fired - the swapchain or blit cannot hold 60 fps after the tile cache and the DComp visual tree exist - and a new ADR, numbered when it is created so `docs-check.py` still indexes every ADR, revisits D3 with the spike's numbers and the failing frame table attached<br>- The spike's code is either kept as the viewer's seed or deleted with the ADR attached. Keeping it "for now" after a negative verdict is how a rejected framework becomes permanent<br>- `docs/kickoff.md` section 12's row "Skia as a second render backend" is updated to point at that ADR once it exists, and `docs/a11y/` gains the two scripts named above rather than a placeholder<br>- `sh tools/check.sh` green on the last commit, and the epic's final gates in `epic-2-technical-tasks.md` run with their output pasted | `docs/kickoff.md` M1 row 5, the kill-criterion paragraph, sections 10 and 12, ADR-0002 `src/os/win32/uia`, ADR-0010, `docs/testing-playbook.md` (Accessibility and Windows-specific rows) |

**Quick traceability:**

| Story | Reference doc                                          | Key aspect |
|-------|--------------------------------------------------------|-----------------------------------------------------|
| 2.1   | `docs/kickoff.md` M1, section 11, ADR-0010             | entry conditions, measurement spine, the three days  |
| 2.2   | `docs/kickoff.md` M1 row 1, ADR-0011 R-M10             | window, DComp swapchain, Direct2D blit at budget     |
| 2.3   | `docs/kickoff.md` M1 row 2, section 10                 | tiles, cachemap, the budget as a core decision       |
| 2.4   | `docs/kickoff.md` M1 row 3, Epic 1 story 1.5 (debt)    | cold start now, the comparison when the bar exists   |
| 2.5   | `docs/kickoff.md` M1 rows 4, 6, 7                      | Per-Monitor V2 DPI, composition and caret, lag       |
| 2.6   | `docs/kickoff.md` M1 row 5, kill criterion, section 12 | reachability, narration, the verdict and its ADR     |

---

*Next step: story 2.1 first - a spike whose harness is not trusted produces numbers nobody can
re-check. Tasks in `epic-2-technical-tasks.md`, level-by-level commands in `epic-2-testing.md`,
evidence in `epic-2-dod.md`. Rows that cannot be measured because an Epic 1 artefact is missing
stay open with their reason; they are not reworded into something measurable.*
