# Epic 1 – Stories (Acceptance) [grounded]

Each AC cites the real file (ADR, doc, tool, workflow) it comes from. The last bullets are
executable checks or gates. `epic-1-dod.md` requires a pasted command output next to every figure.

| # | Story | Acceptance Criteria (grounded) | Real Reference |
|---|-------|--------------------------------|----------------|
| 1.1 | **`main` carries the seed and CI proves it** | <br>- The 18 missing paths (11 `adr/*.md`, `src/features/sidecar/SPEC.md`, `tests/fixtures/sidecar/example.tynypdf.json`, `.editorconfig`, `.gitattributes`, `.githooks/pre-commit`, `.github/PULL_REQUEST_TEMPLATE.md`, `.github/workflows/gates.yml`) and the 5 truncated ones (`.gitignore` 364 B, `CHANGELOG.md`, `docs/lessons.md`, `docs/coding-standards.md`, `docs/kickoff.md`) are on `main`, arrived through a PR, and `git ls-tree -r --name-only main \| wc -l` is 42 or more with the sizes matching the workspace<br>- `git init` + `git config core.hooksPath .githooks` were used; the push was not a directory copy - the loss mechanism is written into `docs/dev-environment.md` or `docs/git-workflow.md` as a lesson so it cannot recur silently<br>- `sh tools/check.sh` exits 0 on the merge commit **on a clean clone of `main`**, not only in the workspace<br>- The `gates` job runs on `main` at least once green and is a required check; run id and head sha pasted<br>- The six settings of `docs/git-workflow.md` are ticked and reported (see `docs/release-runbook.md` section 1 for where each one lives), with branch names validated against `git branch -a` before typing them<br>- One deliberately broken fixture (two-space indentation or a `Tyny Pulse` token) turns the docs job red on a throwaway branch, output pasted, then reverted<br>- A non-maintainer (or a second account/CI token) is denied a direct push to `main`; a review-required rejection is recorded | `docs/kickoff.md` M0.1/M0.1b, `docs/git-workflow.md` "The branch rules", `docs/release-runbook.md` section 1, `.github/workflows/gates.yml`, `tools/{check.sh,canonical-check.py,docs-check.py,naming-sync.py,lang-check.sh}` |
| 1.2 | **Build system and toolchain probes: two compilers, one tree** | <br>- `CMakeLists.txt`, `CMakePresets.json` (`linux-core`, `win-cross-x64`, the `win-msvc/` tree of `docs/dev-environment.md`), `conan.lock`, `.clang-format`, `.clang-tidy`, `.clangd` exist (PR #2's deliverables, including the CI matrix that runs them)<br>- `cmake --preset linux-core && cmake --build --preset linux-core && ctest --preset linux-core` is green with ASan/UBSan enabled<br>- `cmake --preset win-cross-x64 -DCMAKE_BUILD_TYPE=Release` builds and produces the exe named by `docs/naming.md` (`artifact_stem: tynypdf`) at the documented path `build/win-cross-x64/Release/`<br>- The MSVC job runs on a `windows-latest` runner and is required; `winprobe_test` passes under both toolchains on the same target<br>- `tools/win-probe/` (planned, PR #1 per `AGENTS.md`) records the four ADR-0010 results as pasted output: headers compile, `objdump -p` shows only system DLLs under the static runtime, both toolchains build one target, WSL interop reports a hardware adapter LUID - or ADR-0010 is amended with the failing command and its output<br>- `docs/dev-environment.md` section 0's "Unverified" list is shortened to what actually remains unverified, and `docs/coding-standards.md`'s "still prose, because there is no `src/` yet" line is retired together with that sentence<br>- `sh tools/check.sh` green afterwards | ADR-0010, `docs/dev-environment.md` sections 0-2, `docs/kickoff.md` M0.2, PR #2 |
| 1.3 | **The engine is vendored and the supply-chain gates have teeth** | <br>- `third_party/mupdf/` is a submodule pinned at a 40-hex commit recorded in `third_party/UPSTREAM.toml` (ADR-0004 section 1 fixes the keys: `upstream_url`, `upstream_commit`, `vendored_on`, `patch_series`, and every patch carries `Subject`, `Reason`, `Upstream-status`, `Owner`), with each patch as one git-am-applicable file in `third_party/patches/*.patch` (an ordered series, never commits on a private branch)<br>- `tools/deps-refresh.sh` (planned, PR #4) and `tools/patch-report.sh` (planned, PR #4) run in CI and are green; ADR-0011 R-M11's "every upstream commit is reachable" criterion is met or explicitly downgraded<br>- `tools/layering-check.sh` (planned, PR #4) is wired, and its exit code is defined for a tree without `src/core`: it either reports the ratio over the paths that exist or fails with a stated reason - "0 matches" over a missing directory is never a pass (ADR-0011 R-M6)<br>- `tools/sbom.sh` (planned, PR #4) emits a CycloneDX SBOM and a dependency version table that are attached to the release; `NOTICE` lists engine, fonts, third parties<br>- `docs/references.md` exists as the engineering reference list, seeded from the URLs in ADR-0004, ADR-0010, `docs/dev-environment.md` and `docs/release-runbook.md`<br>- No source file includes an engine header outside a backend TU (R-M10); checked now with a grep gate rather than asserted<br>- `sh tools/check.sh` green on the merge commit | ADR-0004 sections 1-2/4, ADR-0011 R-M6/R-M10/R-M11, `docs/kickoff.md` section 8, PR #4 |
| 1.4 | **Page 1 renders twice from one API** | <br>- `include/pdfcore/{status.h,doc.h,page.h,render.h,backend.h,import.h}` and `include/pdfcore/backend/{null.h,mupdf.h}` exist; `pdfcore.h` is an aggregate header; nothing in `pdfcore/` includes an engine header (R-M10)<br>- The API is exactly M0's minimal set: `pc_doc_open`, `pc_doc_page_count`, `pc_page_render` - no `pc_doc_save`, no `pc_doc_sign`, no text model (those are M1/M2 and later)<br>- `pc_status` is `struct { uint32_t size; pc_error code; uint32_t detail_id; const char *detail; }` with `PC_ERR_*` append-only, `detail` pointing into static storage, `struct_size` checked first (ADR-0003 section 1)<br>- A golden test compares `include/pdfcore/status.h` against `tests/golden/status_enum.txt` so an enum renumbering fails the build (R-M12)<br>- `src/backend/null/` is a pure-`pdfcore` implementation that renders a fixed pattern, and `tests/contract/backend_contract.cc` passes against both `null` and `mupdf` (R-M5: an engine-agnostic test is includable without `backend.h`)<br>- The CLI writes one PNG of page 1 with `--page N` and `--backend null\|mupdf`, exiting 0 on success, 1 on a corrupt file, 2 on an argument error, 3 on unsupported (ADR-0003 section 6); `tests/unit/test_cli_exit_codes.cc` asserts all four codes, including corrupt-before-unsupported precedence<br>- On the Linux target, a Win32 window created via the interop path blits the same pixel bytes the CLI wrote; the Win32-side screenshot and the PNG hash are in `epic-1-dod.md`<br>- M0.3's wording is the criterion: same pixels, same API, no duplicated decision logic - "renders page 1 with the same code path" and nothing more | ADR-0002, ADR-0003 sections 1/6, ADR-0011 R-M5/R-M10/R-M12, `docs/kickoff.md` M0.3, PR #5 |
| 1.5 | **The bar is measured before we clear it** | <br>- `tools/bench-measure.sh` (planned, PR #3) with `tests/bench/harness/` and `tests/baseline.json` exists and records M0.4's five metrics - open, first paint, line-by-line scroll, document search, peak RSS - for the pinned corpus on the reference machine, with the machine spec (CPU model, RAM, GPU, `os-release`, compiler version) pasted<br>- Baselines for SumatraPDF **3.6.1 and the 3.7 pre-release** on the same corpus and machine, both labelled with channel, version, acquisition date and where the binaries came from; a run without the pre-release channel does not satisfy M0.4<br>- A second consecutive run reproduces within 10 % on every metric; both runs' outputs are pasted<br>- The acceptance bar is written in relative terms ("within 15 % of 3.7 on open and first paint, faster on search") and M0's exit criterion carries no absolute number, because absolute milliseconds depend on the machine<br>- `tests/conformance/` is a created repository, its default branch protected, and pinned here at a **commit hash, not a branch name**; the intake plan says who screens each source file, the licence, and its status (D13: screening not started)<br>- `sh tools/check.sh` green on the merge commit | `docs/kickoff.md` M0.4/M0.5 and section 6, ADR-0011 R-M3/R-M7, PR #3 |

**Quick traceability:**

| Story | Reference doc                     | Key aspect                                              |
|-------|-----------------------------------|---------------------------------------------------------|
| 1.1   | `docs/kickoff.md` M0.1/M0.1b      | `main` parity, first green CI run, protection, PR flow  |
| 1.2   | ADR-0010, `docs/dev-environment.md` | build system, presets, both toolchains, win probes    |
| 1.3   | ADR-0004, ADR-0011 R-M10/R-M11    | vendored engine pinned, supply-chain and layering gates |
| 1.4   | ADR-0002, ADR-0003 section 1      | `pdfcore` C API, `null` backend, page 1 twice           |
| 1.5   | `docs/kickoff.md` M0.4/M0.5, D12/D13 | Sumatra baseline on both channels, corpus pinned     |

---

*Next step: execute stories 1.1-1.5 in the task order of `epic-1-technical-tasks.md` (1.1 first:
nothing else can be evidenced while `main` is red), pasting evidence into `epic-1-dod.md` as each
step goes green. M0 is an exit criterion, not a phase to admire.*
