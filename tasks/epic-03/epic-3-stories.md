# Epic 3 - Stories (Acceptance) [grounded]

Each AC cites the real file (ADR, doc, tool, SPEC) it comes from. The last bullets
are executable
checks or gates. `epic-3-dod.md` requires a pasted command output next to every
figure.

| # | Story | Acceptance Criteria (grounded) | Real Reference |
|---|-------|--------------------------------|----------------|
| 3.1 | **IR, page boxes and geometry -- no engine crosses the seam** | <br>- `src/core/doc/` and `src/core/geom/` exist with headers under `include/pdfcore/` (`doc.h`, `page.h`, `geom.h`) exposing only value types (`pc_rect`, `pc_page_box`, `pc_text_run`) -- no `fz_*`, no `FPDF_*`, no `windows.h` in any file under `src/core`<br>- `pc_doc` IR owns page geometry: MediaBox, CropBox, rotation 0/90/180/270, user-space origin bottom-left -- conversion `user <-> device` is one function in `geom/` tested for all 4 rotations and CropBox != MediaBox<br>- `pc_page_render` path after this story goes through the IR (backend produces engine pixels -> core copies out into `pc_pixmap` value type, R-M4: no engine object crosses a seam, caller may close doc while holding pixmap)<br>- Page count, page box and a 100×100 render are identical through `null` and `mupdf` backends on the same `simple.pdf` -- contract suite proves vtable is the whole interface (R-M2)<br>- `grep -rEn '#include *[<\"](windows\\|windef\\|d2d1\\|fitz\\|mupdf)' src/core src/render` prints 0; `sh tools/layering-check.sh --strict` exits 0 and prints `backend_line_ratio` <= 0.15 (and after story 3.5 <= 0.07)<br>- `sh tools/check.sh` green | ADR-0002 layout, ADR-0011 R-M2/R-M4/R-M8/R-M10, docs/coding-standards.md §3.1, src/features/sidecar/SPEC.md R3-R6 ownership |
| 3.2 | **Text engine-agnostic -- NFKC, pt-BR, caret over combining marks** | <br>- `src/core/text/` exists with `normalize.h`, `break.h`, `caret.h` -- all NFKC + casefold + whitespace collapse shared with sidecar re-anchoring (ADR-0007 §4) and search<br>- Golden corpus `tests/fixtures/text/ptbr-golden.txt` with combining marks (`a + [combining acute] = a` via U+0301), cedilla, tilde, ABNT2 dead keys -- break and caret positions are approval-tested headless on Linux<br>- `pc_doc_page_text(doc, page, &runs, &count)` returns `pc_text_run` value types (rect, utf8 owned by array, font_face) -- same bytes through `null` and `mupdf` backends for the same logical page (null synthesises a fake run with the same normalisation)<br>- `// R4.*` caret movement: left/right over `e\u0301` is one visual step, not two codepoints -- covered by `tests/unit/test_caret.cc` (or `test_text_break.cc`) with 0 engine headers<br>- No `fitz` text extraction in `src/core` -- only in `src/backends/mupdf/`; `src/core/text` links no engine lib (`ldd`/`dumpbin /DEPENDENTS` of `pdfcore` target lists no `mupdf`)<br>- `sh tools/check.sh` green; `ctest --preset linux-core -R text` green | docs/kickoff.md D-4, ADR-0007 §4 normalisation, ADR-0011 R-M4/R-M8, docs/coding-standards.md §3.3/§3.5 |
| 3.3 | **Budget in the core, sidecar atomicity and hygiene** | <br>- `src/core/budget/` exists: `pc_budget { max_rss_mb, max_tiles, max_sidecar_bytes }` decided here, **read** by `src/render` (render never invents a budget -- R-M8). Eviction policy (LRU + pin) is unit-tested on Linux with no GPU: shrinking the ceiling fails a test that then passes after the fix<br>- `src/core/sidecar/writer.cc` -- R3.1 atomic write: write to `*.tynypdf.json.tmp.<rand>` in same dir -> `fsync` -> `rename` over target; no `*.tynypdf.json` ever truncated on crash (test kills mid-write and verifies previous bytes intact)<br>- R3.2 lock: while `.tynypdf.lock` exists and is fresher than 5 minutes, writer refuses with `PC_ERR_STATE` and message naming the lock age -- `tests/unit/test_sidecar_lock.cc` covers fresh vs stale (>5 min) vs missing<br>- R4.1 unknown keys preserved verbatim (round-trip through newer sidecar fixture) -- `tests/unit/test_sidecar_unknown_keys.cc`<br>- R6.1 view state never written (zoom/page/scroll/window size) -- `tests/unit/test_sidecar_schema.cc` fails if any of `open_page, zoom, scroll, window_size` appear; R6.2 text/geometry/credential never written<br>- `python3 tools/sidecar-fmt.py check tests/fixtures/sidecar` green after every write (byte-canonical: UTF-8, LF, 2-space, keys sorted, 3 decimals, RFC3339 seconds -- ADR-0007 §2)<br>- `sh tools/check.sh` green | src/features/sidecar/SPEC.md R3.1/R3.2/R4.1/R6.1/R6.2, ADR-0007 §2/§5/§6, ADR-0011 R-M8, docs/coding-standards.md §3.7 |
| 3.4 | **Version gate, id validation and staleness -- the 4 remaining sidecar rules** | <br>- R2.2 version gate: `format_version` > supported -> reader loads read-only and returns `PC_ERR_VERSION` with message `"format_version X > supported Y"` -- `tests/unit/test_sidecar_reader.cc`; writer never lowers version<br>- R2.3 id validation: validator rejects any `id` not matching `^[a-z2-7]{10}$` (RFC 4648 base32 lowercase, no padding) -- `tests/unit/test_sidecar_ids.cc` covers too short, uppercase, `1`/`8`/`9`/`0`, padding `=`<br>- R5.1 staleness: when recorded `pages` != `doc_page_count`, sidecar marked stale and `pc_status` detail says `"stale: sidecar pages N vs document M"` -- writes disabled until caller confirms -- `tests/unit/test_sidecar_staleness.cc`<br>- R5.2 fingerprint vs mtime: `document.sha256` is strong signal, `modified` mtime is weak -- status detail names which fired (`"fingerprint mismatch"` vs `"mtime newer"`), golden file `tests/golden/sidecar-stale-report.txt` records exact format -- `tests/unit/test_sidecar_staleness.cc` + golden comparison<br>- Re-anchoring ladder **not** implemented here (that is Epic 5/D-1) -- but the anchor fields (`quote`, `prefix`, `suffix`, `text_sha256`, `score`, `state`) are parsed and preserved, so a sidecar written by a newer version round-trips without loss<br>- `python3 tools/spec-check.py` reports pending 17 -> 7 (R2.2, R2.3, R3.1, R3.2, R4.1, R4.2*, R5.1, R5.2, R6.1, R6.2 closed; *R4.2 "SHALL NOT write for doc it did not open" deferred to Epic 4 if not closed here, but listed as closed if writer checks doc fingerprint)<br>- `sh tools/check.sh` green | src/features/sidecar/SPEC.md R2.2/R2.3/R5.1/R5.2, ADR-0007 §3/§4, docs/sidecar.schema.json |
| 3.5 | **pdfcore API freeze, golden header and contract final -- ratio saneado** | <br>- `include/pdfcore/*.h` frozen for M0: `pdfcore.h` aggregate, `status.h` (`pc_status { size, code, detail_id, detail }` with `PC_ERR_*` append-only 0-15, `detail` static storage), `doc.h` (`pc_doc_open/close/page_count`), `page.h` (`pc_rect`, `pc_render_params`, `pc_pixmap`, `pc_page_render/pixmap_free`), `backend.h` vtable (`abi_major/minor/struct_size` + `doc_open/close/page_count/page_get/page_render/page_free/pixmap_free/get_last_error`), plus new `text.h` and `budget.h` -- `pc_` prefix, brand-free (docs/naming.md)<br>- Golden guard: `tests/golden/status_enum.txt` vs `include/pdfcore/status.h` -- `tests/unit/test_status_abi.cc` fails if any enum number moves; throwaway branch renumbering pasted as red proof in `epic-3-dod.md` (R-M12)<br>- `src/backends/mupdf/exception_bridge.h` is the **only** TU that mentions `fz_try`/`fz_catch` -- `grep -rn "fz_try" src --include="*.cc" --include="*.h" \| grep -v exception_bridge` prints 0<br>- MuPDF backend hardened: per-context isolated lock set (no process-wide mutex array -- the 13.3× bug from `mupdf-rs` #260), allocator ownership R-M6 (backend that allocates frees), `fz_try` bridge tested<br>- Contract suite `tests/contract/backend_contract.cc` green for **both** backends in one `ctest` invocation -- `null` pattern vs `mupdf` pixels share the same IR copy-out; unsupported capability returns `PC_ERR_CAPABILITY` not empty list (R-M5)<br>- `sh tools/layering-check.sh --strict` reports `backend_line_ratio <= 0.07` (~= 180 backend lines / ~2600 total) on this tree -- if it reports higher, the epic is not done<br>- `tests/baseline.json` re-measured: `bench-measure.sh` with `mupdf` backend, Release, no ASan, 5 runs, 2 consecutive runs within 10% per metric, `machine_spec` from `WEXITSTATUS` host, corpus `simple.pdf/multipage.pdf/text.pdf` sha256s pasted -- replaces the `null` Debug numbers<br>- `sh tools/check.sh` + `sh tools/gates-selftest.sh` (13/13) + `ctest --preset linux-core --output-on-failure` green on last commit; `python3 tools/docs-check.py` 0 problems over the 5 new epic docs<br>- `sh tools/check.sh` green | ADR-0003 error model, ADR-0011 R-M3/R-M5/R-M6/R-M7/R-M10/R-M11/R-M12, ADR-0002 layout, docs/kickoff.md M0 exit, docs/naming.md |

**Quick traceability:**

| Story | Reference doc | Key aspect |
|-------|---------------|------------|
| 3.1 | ADR-0002, ADR-0011 R-M4/R-M8/R-M10 | IR, geometry, copy-out |
| 3.2 | docs/kickoff.md D-4, ADR-0007 §4 | NFKC text, pt-BR, caret |
| 3.3 | src/features/sidecar/SPEC.md R3/R4/R6 | Budget, atomic write, lock, preserve, exclusions |
| 3.4 | src/features/sidecar/SPEC.md R2/R5 | Version gate, ids, staleness |
| 3.5 | ADR-0003, ADR-0011 R-M3/R-M11/R-M12 | API freeze, golden, contract, ratio |

---

*Next step: execute stories 3.1-3.5 in the task order of `epic-3-technical-tasks.md`,
pasting
evidence into `epic-3-dod.md` as each gate goes green. IR first -- a text or budget
PR that
precedes the IR will be returned.*
