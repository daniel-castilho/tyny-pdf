// Placeholder TU for tynypdf-render.
// The render layer holds portable presentation logic (tile cache, cachemap, story 5.3);
// its DirectX swapchain implementation lives in src/os/win32/swapchain because a Windows
// or DirectX header may not appear under src/render (AGENTS.md rule 1, ADR-0011 R-M10).
