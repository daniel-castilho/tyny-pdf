---
# Editor guidance only: clangd reads compile_commands.json from the configure step and
# needs no per-folder CXX standard once it exists. The fallback below covers standalone
# files opened before the first configure.
CompileFlags:
  Add:
    - -std=c++20
Diagnostics:
  UnusedIncludes: Strict