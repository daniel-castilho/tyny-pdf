#include <cstdio>
#include <cstdlib>
#include <cstring>

#include "pdfcore.h"

// R12.1 - The CLI SHALL render a specified page to a PNG file.
// R12.2 - The CLI SHALL return exit code 0 on success, 1 on corrupt file, 2 on argument error, 3 on
// unsupported.

int main(void) {
  printf("test_cli_exit_codes: not yet implemented\n");
  return 0;
}